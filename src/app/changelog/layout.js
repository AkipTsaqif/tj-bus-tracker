export const metadata = {
    title: "Daftar Perubahan Aplikasi - Transum App",
    description: "Daftar setiap perubahan (update) dari Transum App",
};

export default function ChangelogLayout({ children }) {
    return <div>{children}</div>;
}
