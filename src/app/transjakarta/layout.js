export const metadata = {
    title: "Tracking Posisi Transjakarta - Transum App",
    description: "Cek lokasi bus Transjakarta di sini",
};

export default function TJLayout({ children }) {
    return <div>{children}</div>;
}
